__pycache__/
*.pyc
*.pyo
*.pyd
.Python
env/
venv/
.venv/
bin/
build/
dist/
develop-eggs/
eggs/
lib/
lib64/
parts/
sdist/
var/
*.egg-info/
.installed.cfg
*.egg
.pytest_cache/
.coverage
htmlcov/
.idea/
.vscode/
# uv.lock should NOT be ignored if we want deterministic builds with --frozen
# uv.lock
# We might want uv.lock if we want deterministic builds, 
# but usually uv sync handles it. 
# Actually, it's better to include it for --frozen.
# .env stays out
.env
server.log
*.log
